============
Installation
============

At the command line::

    $ easy_install django_busybody

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_busybody
    $ pip install django_busybody
